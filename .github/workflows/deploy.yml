name: Deploy to VM
on:
  push:
    branches: [ "main" ]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Update Service
        run: |
          cd ~/PEI/RUA-Backend-Services
          bash pull.sh
          docker compose up -d --build waittime-service