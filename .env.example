# Database Configuration
DATABASE_URI=postgresql://postgres:postgres@localhost:5432/estadio_do_dragao

# API Authentication
# Load from environment variable for production; development falls back to default
API_KEY=dragao_secret_key_2026
